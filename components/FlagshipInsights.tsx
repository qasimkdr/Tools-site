import type { FlagshipContent } from "@/lib/flagship-content";

export function FlagshipInsights({content}:{content:FlagshipContent}){return <div className="flagship-insights" data-insight-tier={content.curated?"curated":"tool-specific"}><h2>How to interpret your result</h2><p>{content.decision}</p><h2>Input guide</h2><div className="insight-list">{content.inputs.map((text,index)=><div key={text}><b>{index+1}</b><p>{text}</p></div>)}</div><h2>Scenario comparison</h2><div className="guide-table-wrap"><table><thead><tr><th>Scenario</th><th>What it demonstrates</th></tr></thead><tbody>{content.scenarios.map((scenario,index)=>{const [label,...rest]=scenario.split(" — ");return <tr key={scenario}><td>{label}</td><td>{rest.join(" — ")||`Scenario ${index+1}`}</td></tr>})}</tbody></table></div><h2>Common mistakes to avoid</h2><ul className="mistake-list">{content.mistakes.map(text=><li key={text}>{text}</li>)}</ul><div className="verification-box"><b>How to verify this result</b><p>{content.verification}</p></div></div>}
